interface Book {
    tittle: string
    // isActive: boolean
    // emailIds?: string[]
    isfunction(): string
}

const myInterf: Book = {
    tittle: "t",
    isfunction() {
        return 1
    }
}
